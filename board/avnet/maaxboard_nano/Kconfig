if TARGET_MAAXBOARD_NANO

config SYS_BOARD
	default "maaxboard_nano"

config SYS_VENDOR
	default "avnet"

config SYS_CONFIG_NAME
	default "maaxboard_nano"

config IMX8MN_LOW_DRIVE_MODE
	bool "Enable the low dirive mode of maaxboard_nano"
	default n

source "board/freescale/common/Kconfig"

endif
